{% extends "::layout/layoutAdmin.html.twig" %}

{% block body -%}
    <h1>Modification de cet avis</h1>
    <div class="row">
    	<div class="col-xs-offset-1 col-xs-2">Produit :</div><div class="col-xs-3">{{avis.produit.nom}}</div>
    </div>
    <div class="row">
    	<div class="col-xs-offset-1 col-xs-2">Date :</div><div class="col-xs-3">{{avis.date|date('d-m-Y')}}</div>
    </div>
    <div class="row">
    	<div class="col-xs-offset-1 col-xs-2">Utilisateur :</div><div class="col-xs-3">{{avis.user}}</div>
    </div>
    <div class="row">
    	<div class="col-xs-offset-1 col-xs-2">Note sur 5 :</div><div class="col-xs-3"> {{ form_widget(edit_form.stars) }}</div>
    </div>
    <div class="row">
    	<div class="col-xs-offset-1 col-xs-2">Titre :</div><div class="col-xs-3"> {{ form_widget(edit_form.titre) }}</div>
    </div>
    <div class="row">
    	<div class="col-xs-offset-1 col-xs-2">Commentaire :</div><div class="col-xs-3"> {{ form_widget(edit_form.comment) }}</div>
    </div>
    <div class="row">
    	<div class="col-xs-offset-1 col-xs-2">Validation :</div><div class="col-xs-3"> {{ form_widget(edit_form.valid) }}</div>
    </div>
    <br/>
    <div class="row">
    	<div class="col-xs-3"> 
	    	<a href="{{ path('adminAvis') }}" class="btn btn-greyish ">
	            Retour à la liste
	        </a>
	    </div>  
    	<div class="col-xs-3"> {{ form_widget(edit_form.submit) }}</div>
    	<div class="col-xs-3 ">  {{ form(delete_form) }}</div>
    </div>
    {#{ form(edit_form) }#}



   
</div>
{% endblock %}
