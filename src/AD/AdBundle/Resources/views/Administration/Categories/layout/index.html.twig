{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Liste des categories</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th colspan="2">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('adminCategories_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>
                        <a href="{{ path('adminCategories_show', { 'id': entity.id }) }}"><i class="glyphicon glyphicon-eye-open"></i></a>
                </td>
                <td>
                        <a href="{{ path('adminCategories_edit', { 'id': entity.id }) }}"><i class="glyphicon glyphicon-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="row">
        <div class="col-xs-offset-2 col-xs-2"  >
        <a href="{{ path('adminProduits') }}">
            <button type="button" class="btn btn-greyish" >RETOUR LISTE DES PRODUITS</button>
        </a>
        </div>
        <div class="col-xs-offset-2 col-xs-2"  >
        <a href="{{ path('adminCategories_new') }}">
            <button type="button" class="btn btn-success" >AJOUTER UN NOUVELLE CATÉGORIE</button>
        </a>
        </div>
    </div>

    {% endblock %}
