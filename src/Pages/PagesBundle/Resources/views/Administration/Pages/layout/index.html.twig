{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Liste des pages du footer</h1>

    <table class="table table-striped table-hover" >
        <thead>
            <tr>
                <th>Titre</th>
                <th colspan="2">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.titre }}</td>
                <td>
                        <a href="{{ path('adminPages_show', { 'id': entity.id }) }}" title="voir" ><i class="glyphicon glyphicon-eye-open"></i></a>
                </td>
                <td>
                        <a href="{{ path('adminPages_edit', { 'id': entity.id }) }}" title="éditer" ><i class="glyphicon glyphicon-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table><br/><br/>
    <div class="row">
        <div class="col-xs-offset-2 col-xs-2"  >
        <a href="{{ path('adminProduits') }}">
            <button type="button" class="btn btn-greyish" >RETOUR LISTE DES PRODUITS</button>
        </a>
        </div>
        <div class="col-xs-offset-2 col-xs-2"  >
        <a href="{{ path('adminPages_new') }}">
            <button type="button" class="btn btn-success" >AJOUTER UNE NOUVELLE PAGE DANS LE FOOTER</button>
        </a>
        </div>
    </div>

    {% endblock %}
